# Ejemplo de cabecera 
n = 0
while n < 10: # While = Mientras
    if (n % 2) == 0: # if = si (de condicion)
        print(n, "Es un número par")
    else: # ELse = si no (define como una condicion)
        print(n, "Es un número impar")
    n = n+1